# Root Finding


Iterative methods for solving $f(x) = 0$ for continuous $f : \fR \to \fR$.

## Bracketing Methods

Guaranteed convergence to a root within a known interval.


::: {.callout-note icon=false}
## Theorem: Bisection Method
Given $f \in C[a, b]$ with $f(a)f(b) < 0$, a root $x^* \in (a, b)$ exists.
The method repeatedly halves the interval:

1. $m = (a + b)/2$
    
1. If $f(a)f(m) < 0$, new interval is $[a, m]$, else $[m, b]$.

**Error Bound:**  After $n$ steps, $|x_n - x^*| \leq (b - a)/2^n$.
:::


::: {.callout-warning icon=false}
## Exercise

1. Use the intermediate value theorem to prove that the initial bracket contains a root.
    
1. Show that the bisection update preserves the sign-change bracket.
    
1. Prove that the bracket length after $n$ steps is $(b-a)/2^n$.
    
1. Derive the error bound in the result above.

:::


::: {.callout-tip icon=false}
## Remark
**(Bisection trade-off)**  Bisection is robust because it preserves a bracket, but its convergence is only linear. Each step gains exactly one bit of accuracy.
:::


## Open Methods

Faster local convergence, but sensitive to the initial guess $x_0$ and may diverge.


::: {.callout-note icon=false}
## Definition: Newton's Method
Linearizes $f$ at the current iterate:
$$
\begin{align}
x_{k+1} = x_k - \frac{f(x_k)}{f'(x_k)}.
\end{align}
$$
:::


::: {.callout-note icon=false}
## Theorem: Quadratic Convergence
If $f''(x)$ is continuous near a simple root $x^*$ ($f'(x^*) \neq 0$), Newton's method converges quadratically:
$$
\begin{align}
|x_{k+1} - x^*| \approx \frac{|f''(x^*)|}{2|f'(x^*)|} |x_k - x^*|^2.
\end{align}
$$
:::


::: {.callout-warning icon=false}
## Exercise

1. Expand $f(x^*)$ about $x_k$ using a second-order Taylor formula.
    
1. Divide by $f'(x_k)$ and isolate the Newton correction $f(x_k)/f'(x_k)$.
    
1. Substitute $x_{k+1}=x_k-f(x_k)/f'(x_k)$.
    
1. Show that $|x_{k+1}-x^*| \leq C|x_k-x^*|^2$ once $x_k$ is close enough to a simple root.
    
1. Explain why the assumption $f'(x^*)\neq 0$ is essential.

:::


::: {.callout-tip icon=false}
## Remark
**(Newton failure modes)** 
1. $x_0$ too far from $x^*$.
2. $f'(x_k) \approx 0$ (zero derivative).
3. Multiple roots ($f'(x^*) = 0$): convergence degrades to linear with rate $1/2$.
:::


::: {.callout-note icon=false}
## Definition: Secant Method
Approximates $f'$ via finite differences of the last two iterates:
$$
\begin{align}
x_{k+1} = x_k - f(x_k)\frac{x_k - x_{k-1}}{f(x_k) - f(x_{k-1})}.
\end{align}
$$
Requires two initial points ($x_0, x_1$).
:::


::: {.callout-note icon=false}
## Theorem: Superlinear Convergence
The secant method converges with order $\phi = (1 + \sqrt{5})/2 \approx 1.618$ (the golden ratio).
**Efficiency:**  Faster than bisection, slower than Newton, but requires only 1 function evaluation per step (Newton requires 2: $f$ and $f'$).
:::


::: {.callout-warning icon=false}
## Exercise

1. Derive the secant update by replacing $f'(x_k)$ in Newton's method with a finite difference through $(x_{k-1},f(x_{k-1}))$ and $(x_k,f(x_k))$.
    
1. Compare the information used by Newton and secant.
    
1. Numerically estimate the convergence order for $f(x)=x^2-2$.

:::


## Brent's Method

A standard robust algorithm for one-dimensional root-finding.


::: {.callout-note icon=false}
## Theorem: The Hybrid Approach
Brent's method combines:

1. **Bisection:**  For guaranteed global convergence.
    
1. **Secant:**  For fast local convergence.
    
1. **IQI:**  Inverse Quadratic Interpolation using 3 points.

It uses fast open steps by default, falling back to bisection only if the step is outside the bracket or not improving quickly enough.
:::


::: {.callout-tip icon=false}
## Remark
**Implementation:**  Used in `scipy.optimize.brentq`.
:::


## Exercises


::: {.callout-warning icon=false}
## Exercise

1. Bisection on $f(x) = x^3 - 2$ on $[1, 2]$ for 4 steps. Find the guaranteed error using the result above.
    
1. Newton on $f(x) = x^2 - 2$ from $x_0 = 1$. Verify the doubling of digits using the result above.
    
1. Prove that for $f(x) = x^2$, Newton converges to $0$ with linear rate $1/2$.
    
1. Find the root of $e^x = 3x$ near $x=1$ via Secant method. Compare evaluations vs.\ Newton.

:::
